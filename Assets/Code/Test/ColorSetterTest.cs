using UnityEngine;
using System.Collections;

public class ColorSetterTest : MonoBehaviour {
	public ColorSetter cs = null;
	// Use this for initialization
	void Start () {
		cs.SetColor (null);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
